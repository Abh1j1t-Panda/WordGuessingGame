# WordGuessingGame
This is a Java game, which lets you guess a given word with 10 attempts or less, at each attempt,  the program tells the number of correct guesses the user has made. Using this knowlege, the user must find the correct word. The Random word is fetched using a public API.
